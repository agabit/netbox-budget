from setuptools import find_packages, setup

setup(
    name='netbox-budget',
    version='0.1',
    description='Budget Planning plugin for NetBox',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
